const { Route } = require("express");
const express = require("express");
const router = express.Router();


//Gives all serach result form the database
router.get("/", async (req, res) => {
  
});



module.exports = router;